pre-commit:
  parallel: true
  commands:
    lint:
      glob: "src/**/*.{ts,tsx}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    format:
      glob: "src/**/*.{ts,tsx,css}"
      run: bunx biome format --write {staged_files}
      stage_fixed: true
