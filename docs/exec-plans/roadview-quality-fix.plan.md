# 구현 계획: 로드뷰 품질 개선 (엉뚱한 로드뷰 교정)

> 이 문서는 [로드뷰 panoId 전수 수집](./roadview-panoid-full-backfill.plan.md)을 대체한다.
> 기존 계획은 "좌표 자동 탐색 결과를 그대로 저장"하는 것이 목표였는데,
> 그렇게 저장하면 **지금 화면에 나오는 잘못된 로드뷰가 그대로 고정될 뿐**이라는 것이
> Phase B spike에서 확인되었다. 목표를 "올바른 로드뷰를 고르는 것"으로 재정의한다.

## 문제 정의

주차장 상세 페이지의 로드뷰가 주차장·출입구와 무관한 곳을 비춘다.

대표 사례: **이마트타운 월계점 주차장(`KA-20562951`)** — 트레이더스 월계점이 입점한 건물.
현재 화면에는 건물 뒤편 이면도로의 **빈 외벽**만 나온다.

## 원인 (2026-08-03 실측)

세 가지 원인이 겹쳐 있고, **하나만 고쳐서는 해결되지 않는다.**

### 1. 주차장 좌표가 부지 중심점이다

`KA-20562951`의 등록 좌표 `37.6265483, 127.0618877`은 출입구가 아니라 건물/부지 중심에 가깝다.
카카오 POI 기반 좌표의 일반적 특성이다.

### 2. `Panorama`가 "가장 가까운" 파노라마를 고른다

부지 중심에서 가장 가까운 파노라마는 **건물 뒤 이면도로**(57.9m)다.
주변 파노라마 33개를 반경 0~150m로 프로브한 결과, 실제 정문을 비추는 파노라마는
**84.5m 지점**에 있었다. 즉 **거리와 품질이 역상관**이다.

| 후보 | 거리 | 촬영일 | 화면 내용 |
|---|---|---|---|
| 현재 자동 선택 | 57.9m | 2025-01-22 | 건물 뒤편 빈 외벽 (❌) |
| **#16** | **84.5m** | **2026-02-23** | **이마트타운 정문 출입구·하차장 (✅)** |
| #18 | 86m | 2026-02-23 | 건물 정면, 버스에 가림 (△) |
| #28 | 156m | 2026-02-23 | 전혀 다른 건물의 하역장 (❌) |

### 3. POV가 정북으로 고정되어 있다

`src/components/WikiMiniMap.tsx`가 `pov: { pan: 0, tilt: 0, fov: 100 }`로 파노라마를 띄운다.
`pan: 0`은 **정북**이므로, 주차장이 파노라마 지점의 남쪽에 있으면 반대편을 보게 된다.
월계점은 주차장 방위가 36.6°라 약 37° 어긋난다.

단, **POV만 보정하면 오히려 나빠진다** — 잘못된 파노라마에서 벽을 더 정면으로 볼 뿐이다.
파노라마 선택과 POV는 **함께** 고쳐야 한다.

## 핵심 결정

### 1. "가장 가까운 파노라마" 대신 후보 탐색 + 선택 규칙

주차장 좌표 하나로 조회하지 않고, 주변을 프로브해 후보 파노라마 집합을 만든 뒤 고른다.

프로브: 반경 `0/25/50/75/100/150m` × 8방위 = 41개 좌표 → 유니크 panoId 수집
(월계점 기준 33개, 약 60초 소요)

### 2. 선택 규칙: 근거리 우선 → 아니면 촬영일 최신 그룹 → 최근접

```
if (최근접 거리 <= NEAR_ENOUGH_M)                → 최근접 후보
else if (선택 거리 - 최근접 거리 <= MAX_JUMP_M)   → 촬영 날짜 최신 그룹 → 그중 최근접
else                                              → 최근접 후보
```

`NEAR_ENOUGH_M = 30`, `MAX_JUMP_M = 60`

**날짜 그룹핑 근거**: 네이버는 간선도로·건물 정면을 자주 재촬영하고, 이면도로·서비스도로는
오래된 촬영본이 남는다. 월계점의 뒷골목은 2025-01, 정면 도로는 2026-02 촬영이었다.

**근거리 가드 근거**: Phase 2 검증에서 날짜 규칙 단독은 **역효과를 냈다**.
최근접 후보가 이미 2.4m인 노상주차장에서 5일 더 최신이라는 이유로 109m 떨어진
전혀 다른 건물 앞 버스정류장을 골랐다. 최근접이 이미 주차장 바로 앞이면 그대로 쓴다.

**주의**: 날짜가 아니라 시각(datetime)까지 최신으로 정렬하면 같은 날 더 늦게 촬영된
광운로(14:15)가 뽑혀 **전혀 다른 건물**이 나온다. 반드시 **날짜 단위로 그룹핑**한다.

#### Phase 2 검증 결과 (2026-08-05, 15곳)

대형시설(백화점·마트·터미널·공원) + 서울 노상 혼합 샘플. 프로브 격자는 축소본
(반경 0/30/60/100m × 6방위 = **19회**)을 사용했고 lot당 평균 **1.6초**였다.

날짜 규칙만 적용했을 때 최근접과 다른 선택을 한 6곳의 육안 검수 결과:

| 주차장 | 최근접 → 규칙 선택 | 판정 |
|---|---|---|
| 이마트 월계점 | 56.2m(2025-01) → 86m(2026-02) | ✅ 뒷골목 벽 → 정문 |
| 홈플러스 영등포점 | 67.8m(2026-02) → 84.2m(2026-03) | ✅ 길 건너 타 건물 → 홈플러스 본관 |
| 현대백화점 대구점 | 61.4m(2026-03-24) → 62.2m(2026-03-25) | ➖ 차이 미미 |
| 성산포항여객선터미널 | 8.6m(2019) → 25.1m(2025) | ➖ 둘 다 주차장 내부 |
| 아이파킹 이마트 양재점 | 1.9m(2026-01) → 29.1m(2026-04) | ❌ 주차 진입부 → 교차로 |
| 도산대로45길(노상) | 2.4m(2026-01-16) → 109m(2026-01-21) | ❌ 실제 골목 → 타 건물 버스정류장 |

**개선 3 / 무해 2 / 개악 2**. 개악 2건은 모두 최근접이 3m 미만이었고, 개선 케이스는
모두 최근접이 56m 이상이었다. 30m 임계값이 두 군을 깨끗하게 가른다.

#### Phase 2b — 표본 확대 재검증 (2026-08-05, 60곳 추가 / 누적 73곳)

15곳 표본이 대형시설에 치우쳐 있어, 일반 주차장(노외 35·노상 15·부설 10) 60곳을 추가했다.
**결론이 달라졌다.**

**(1) 규칙이 필요한 lot이 생각보다 적다**

최근접 거리 분포: p25 8.8m · p50 15.9m · p75 28.2m.
**최근접이 30m를 넘는 lot은 73곳 중 16곳(22%)뿐**이다.
나머지 78%는 이미 주차장 바로 앞 파노라마가 잡히므로 규칙이 개입할 이유가 없다.
월계점처럼 "최근접이 엉뚱한 뒷골목"인 상황은 실재하지만 흔하지 않다.

**(2) 30m 가드만으로는 부족하다 — 점프 상한이 필요하다**

30m를 넘겨 규칙이 적용된 케이스 중에도 개악이 나왔다.

| 주차장 | 최근접 → 선택 | 판정 |
|---|---|---|
| 롯데백화점일원 | 35.4m → 101.5m | ❌ 백화점 정문 → 고가도로 밑 |
| 꿈빛도서관 옆 | 34.7m → 108.1m | ❌ (동일 패턴) |

둘 다 **+66m 이상 멀어지는 점프**였다. 반면 정상 개선 케이스는 점프가 +0.8~+43m에 그친다.
그래서 `MAX_JUMP_M = 60`을 추가했다.

**(3) 최종 규칙의 실제 영향 범위**

누적 73곳에 최종 규칙(30m 가드 + 60m 점프상한)을 적용하면 **6곳(8%)만 바뀐다.**

| 주차장 | 최근접 → 선택 | 육안 검수 |
|---|---|---|
| 이마트 월계점 | 56.2m → 86m | ✅ 확인 |
| 홈플러스 영등포점 | 67.8m → 84.2m | ✅ 확인 |
| 현대백화점 대구점 | 61.4m → 62.2m | ➖ 차이 미미 |
| 노비아갈라웨딩 | 34.1m → 49m | 미검수 |
| 복사골문화센터 | 47.1m → 90.4m | 미검수 |
| 수주도서관 | 76.9m → 112.3m | 미검수 |

개악 2건은 점프 상한이 차단한다.

**판단 재료**: 전수 적용 비용은 약 5.3시간(동시성 3) + 네이버 호출 61만 회인데,
**실제로 화면이 개선되는 것은 8% 수준**이다. Phase 3 착수 여부를 이 비율로 다시 판단할 필요가 있다.
대안으로 "최근접 30m 초과 lot(22%)만 프로브" 하면 비용이 1/5로 줄고 효과는 동일하다.

### 3. `title`(도로명)은 선택 신호로 쓰지 않는다

정문을 비추는 #16의 `title`은 빈 문자열이고, 엉뚱한 #28은 `광운로`였다.
도로명 유무는 품질과 무관하다. 리포트 표시용으로만 쓴다.

### 4. POV는 파노라마 → 주차장 방위각으로 계산한다

```
pan = 정방위각(파노라마 좌표 → 주차장 좌표)
```

기본은 런타임 계산(추가 컬럼 불필요)이되, 수동 교정한 경우를 위해 저장 가능하게 둔다.

### 5. 자동 선택은 "개선"이지 "정답"이 아니다 — 검수 경로를 함께 만든다

자동 규칙으로 대부분을 개선하되, 대표 주차장은 스크린샷 기반으로 검수하고
수동으로 panoId·POV를 덮어쓸 수 있어야 한다.

## 데이터 모델

`parking_lots`에 추가 (기존 계획의 컬럼 + 선택 근거·수동 교정용).

```sql
ALTER TABLE parking_lots ADD COLUMN roadview_pano_id TEXT;
ALTER TABLE parking_lots ADD COLUMN roadview_lat REAL;
ALTER TABLE parking_lots ADD COLUMN roadview_lng REAL;
ALTER TABLE parking_lots ADD COLUMN roadview_distance_m REAL;
ALTER TABLE parking_lots ADD COLUMN roadview_photodate TEXT;
ALTER TABLE parking_lots ADD COLUMN roadview_pov_pan REAL;      -- NULL이면 런타임 계산
ALTER TABLE parking_lots ADD COLUMN roadview_status TEXT;
ALTER TABLE parking_lots ADD COLUMN roadview_source TEXT;       -- auto | manual
ALTER TABLE parking_lots ADD COLUMN roadview_candidate_count INTEGER;
ALTER TABLE parking_lots ADD COLUMN roadview_checked_at TEXT;

CREATE INDEX IF NOT EXISTS idx_parking_lots_roadview_status
  ON parking_lots(roadview_status);
```

| 상태 | 의미 |
|---|---|
| `available` | 후보 탐색 후 규칙으로 선택 완료 |
| `unavailable` | 주변에 파노라마가 없음 |
| `needs_review` | 후보가 1개뿐이거나 거리·촬영일이 의심스러움 |
| `error` | SDK/네트워크/예외 |

`roadview_source = 'manual'`인 row는 재수집이 **덮어쓰지 않는다**.

## 단계별 실행

### Phase 1 — POV 보정 (선행, 저비용)

- [ ] `WikiMiniMap.tsx`의 `pan: 0` 고정을 파노라마→주차장 방위각 계산으로 교체
- [ ] `pano_status = OK` 이후 `getLocation()`으로 실제 파노라마 좌표를 얻어 pan 계산
- [ ] 기존 로드뷰 탭 회귀 확인

이 단계만으로 "파노라마는 맞는데 엉뚱한 방향" 케이스가 해결된다.
월계점처럼 파노라마 자체가 틀린 경우는 Phase 2가 필요하다.

### Phase 2 — 선택 규칙 검증 ✅ 완료 (2026-08-05)

- [x] 대표 주차장 15곳(대형마트·터미널·공원·도심 노상 혼합)으로 후보 프로브 실행
- [x] 규칙과 최근접이 다른 케이스 before/after 스크린샷 생성·육안 검수
- [x] 규칙 보정 — **근거리(30m) 가드 추가**. 날짜 규칙 단독은 개악 2건 발생
- [x] 프로브 격자 튜닝 — 19회로 축소, lot당 1.6초 (41회 가정은 과대 추정이었음)

추가 확인 필요: 30m 임계값은 15곳 표본에서 두 군을 가른 값이다.
Phase 3 실행 중 `needs_review` 분포를 보고 재조정할 수 있게 상수로 둔다.

### Phase 3 — 수집기 + 스키마

- [ ] migration + drizzle schema
- [ ] `scripts/collect-roadview-panoid.ts` — 프로브·선택·flush·재개
- [ ] Phase B spike에서 확인된 제약 반영:
      인스턴스 재사용 시 실패하면 이전 panoId가 남으므로 `pano_status = OK`일 때만 읽고,
      이벤트 미수신(약 0.33%)은 새 인스턴스로 재시도 후 `getPanoId()` 폴링
- [ ] 정상 좌표 canary를 주기적으로 섞어 전체 실패 조기 감지

### Phase 4 — 상세 페이지 연결

- [ ] 저장된 panoId + POV 우선 사용, 실패 시 좌표 fallback
- [ ] `roadview_source = 'manual'` 우선순위 보장

### Phase 5 — 검수

- [ ] 상위 주차장 스크린샷 리포트 생성
- [ ] 수동 교정 경로 (관리자 화면 또는 스크립트 입력)

## 적용 범위

Phase 2 실측으로 비용이 확정됐다: 축소 격자(19 프로브) 기준 lot당 **1.6초**.
전수 31,994곳이 동시성 3에서 약 **4.8시간**으로, 한 번에 돌릴 수 있는 규모다.
(계획 초안의 "lot당 60초 = 비현실적"은 스크린샷 시간을 프로브 비용으로 오인한 과대 추정이었다)

따라서 범위를 좁힐 이유가 없어졌다. **전수 적용**을 기본으로 하되,
네이버 호출량이 lot당 19회 × 3.2만 = 약 61만 회이므로 약관·사용량 확인이 선행되어야 한다.

## ⚠️ 약관·사용량 조사 결과 (2026-08-05) — 착수 전 해소 필요

### 1. 결과 데이터 저장·DB화 금지 조항 (계획의 전제를 흔듦)

네이버 클라우드 AI·Naver API 서비스 이용약관에 다음 취지의 조항이 있다고 복수 출처가 일치해서
전한다.

> 모든 Maps API의 결과 데이터는 값을 리턴 받는 즉시 1회 자신의 서비스에서 사용하는 것만
> 허용되며, 결과 데이터를 별도로 저장하거나 데이터베이스화하여 이용해서는 안 된다.

**검증 한계**: 약관 원문 PDF를 로컬에서 파싱하지 못했고(도구 부재), NCP 문서 사이트의
Maps 가이드 페이지들에는 해당 문구가 없다. 즉 **1차 출처로 확인하지 못했다.**
법적 판단이 걸린 사안이므로 이 문서의 서술을 근거로 진행하지 말 것.

이 조항이 문언 그대로라면 `roadview_pano_id`·`roadview_lat/lng`·`roadview_photodate`를
D1에 적재하는 이 계획의 핵심 메커니즘이 **그대로는 불가**하다.

**우회안 (검토 필요)**: Naver의 결과값(panoId)을 저장하지 않고,
**우리가 산출한 "로드뷰 기준 좌표"만 저장**하는 방식. 상세 페이지는 그 좌표로 기존과 동일하게
`position` 기반 Panorama를 띄운다. 저장물이 네이버 결과 데이터가 아니라 자체 파생 좌표라는
해석이 가능하지만, 그 좌표 자체가 `getLocation()`에서 유래하므로 회색지대다.
네이버에 확인이 필요하다.

### 2. 과금 단위 — Panorama 기준 불명

확인된 것:
- "Maps API는 요청된 호출수 기준으로 종량 요금제"
- Web Dynamic Map: **지도 로딩 시에만 1건**, 로딩 후 zoom·marker 등 추가 조작은 미포함
- Mobile Dynamic Map: 뷰 1회 생성(`onCreate`) 시 1건

확인 못 한 것: **Panorama의 1건 정의**. 수집기는 Panorama 인스턴스 1개를 만들고
`setPosition()`으로 19회 이동하는데, 이것이 1건인지 19건인지에 따라 전수 호출량이
**3.2만 건 vs 61만 건**으로 20배 차이 난다.

### 3. 무료 이용량 — Panorama 포함 여부 불명

무료 이용량은 **대표 계정에 한해** 제공되며, 목록으로 확인된 것은
Web Dynamic Map / Static Map / Geocoding / Reverse Geocoding이다.
**Panorama는 이 목록에 없다**(검색 출처 기준, 1차 확인 못 함).

### 착수 전 네이버 클라우드에 문의할 것

1. Panorama `getLocation()` 결과(panoId·좌표·촬영일)를 자사 DB에 저장해도 되는가.
   안 된다면, 파생 좌표만 저장하는 방식은 허용되는가
2. Panorama의 과금 1건 정의 — `setPosition()` 재호출이 건수에 포함되는가
3. Panorama에 무료 이용량이 있는가, 있다면 한도는 얼마인가
4. 헤드리스 브라우저를 통한 자동 호출이 허용되는가

**1번 답변에 따라 이 계획은 설계를 바꾸거나 폐기해야 할 수 있다.**

## 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 선택 규칙이 샘플 1건에서 유도됨 | Phase 2에서 20~30곳 검증 후 확정 |
| panoId 고정 시 네이버 갱신본을 못 따라감 | `roadview_checked_at` 기반 재검증 주기, 실패 시 좌표 fallback |
| 프로브 비용이 큼 | 적용 범위 축소, 프로브 개수 튜닝 |
| 자동 규칙이 여전히 틀린 곳 | `needs_review` + 수동 교정 경로 |
| 네이버 약관·사용량 | 전수 실행 전 확인 (기존 계획에서 이월된 미해결 항목) |

## 범위 제외

- 주차장 입구 좌표 자체를 자동 산출하는 기능
- 로드뷰 이미지 다운로드·자체 호스팅
- 여러 방향·여러 시점 파노라마를 모두 저장하는 기능

## 완료 조건

- [ ] 월계점(`KA-20562951`) 상세 페이지에 정문 출입구가 보인다
- [ ] Phase 2 검증 샘플에서 자동 규칙 적중률이 측정·기록된다
- [ ] 선택 결과에 근거(거리·촬영일·후보 수)가 남는다
- [ ] 수동 교정이 자동 재수집에 덮이지 않는다
- [ ] 테스트, lint, production build 통과
